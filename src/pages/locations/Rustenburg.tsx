import { LocationPage } from "@/components/LocationPage";
import { locationData } from "@/data/locations";

const Rustenburg = () => {
  return <LocationPage {...locationData["rustenburg"]} />;
};

export default Rustenburg;







